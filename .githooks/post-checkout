#!/usr/bin/env bash
[ "$3" = "1" ] || exit 0
branch="$(git symbolic-ref --short HEAD 2>/dev/null || echo DETACHED)"
if [ "$branch" != "main" ]; then
  echo "" >&2
  echo "WARNING: now on '$branch' (not main). Commits here will be refused." >&2
  echo "Switch back: git checkout main" >&2
fi
exit 0
