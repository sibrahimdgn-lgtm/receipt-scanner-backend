#!/usr/bin/env bash
branch="$(git symbolic-ref --short HEAD 2>/dev/null || echo DETACHED)"
if [ "$branch" != "main" ]; then
  echo "BLOCKED by .githooks/pre-commit: refusing commit on '$branch' (not main)." >&2
  echo "Switch back: git checkout main" >&2
  exit 1
fi
exit 0
